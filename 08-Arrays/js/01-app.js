const numeros = [10, 20, 30]; 

//No es tan comun
const meses = new Array('enero', 'febrero','marzo');

console.log(numeros);

//un arreglo que contiene datos de todo tipo
const deTodo = ['Hola',10,true, 'si', null, {nombre:'lilia', trabajo:'programador'}];
console.log(deTodo);
